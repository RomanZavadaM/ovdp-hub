name: ovdp_hub
description: ОВДП Hub — локальний каталог і сценарії інвестування.
publish_to: none
version: 0.8.1+9
environment:
  sdk: '>=3.9.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.0.3
  path: ^1.9.1
  path_provider: ^2.1.5
  decimal: ^3.2.4
  http: ^1.5.0
  flutter_bloc: ^9.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/nbu-snapshot.json
